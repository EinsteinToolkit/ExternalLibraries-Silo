# make.configuration.defn file for thorn Silo

# Define standard Silo utilities and install those that we could build/that are
# present in the system
STD_SILO_UTILS = 
ALL_UTILS += $(STD_SILO_UTILS)
